server:
  port: ${REST_SERVICE_PORT:8086}
spring:
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5433/pogreb}
    username: ${POSTGRES_USERNAME:rembo}
    password: ${POSTGRES_PASSWORD:194827}
  jpa:
    generate-ddl: true

salt: ${SALT:test}